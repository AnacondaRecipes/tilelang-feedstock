# TVM is compiled from the bundled source tree; allow a long build
task_timeout: 28800

# TEMPORARY — remove before merge: staging channel for the fixed libz3/z3-solver
# (AnacondaRecipes/z3prover-feedstock#2); the released libz3-4.15.4_0 carries a
# spurious python_abi cp314 dep that breaks py3.10-3.13 host solves.
channels:
  - https://staging.continuum.io/pbp/fs/z3prover-feedstock/pr2/c099066
